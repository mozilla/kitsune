# retrieval — RAG retrieval layer (in progress)

Production RAG retrieval layer (semantic search first; other applications later). In-process, ES-native hybrid chunk index; application-side embeddings. 
