apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sumo.fullname" . }}-test-connection"
  labels:
    {{- include "sumo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sumo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
