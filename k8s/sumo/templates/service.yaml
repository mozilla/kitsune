apiVersion: v1
kind: Service
metadata:
  name: {{ include "sumo.fullname" . }}
  labels:
    {{- include "sumo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sumo.selectorLabels" . | nindent 4 }}
